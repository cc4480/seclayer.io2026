{
  "name": "tier3-baas-supabase-fixture",
  "version": "1.0.0",
  "private": true,
  "description": "Tier 3 vulnerable-app benchmark fixture (Supabase BaaS) — local use only, never expose to the internet.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
