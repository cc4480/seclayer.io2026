// Hand-maintained rather than imported from package.json — this avoids JSON
// import-assertion syntax differences across TS/Node versions for a single
// constant. Keep this in sync with package.json's "version" field.
export const VERSION = "1.0.0";
